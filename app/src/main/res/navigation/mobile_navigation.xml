<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_camera_control">

    <fragment
        android:id="@+id/navigation_camera_control"
        android:name="com.ptzcontroller.ui.control.CameraControlFragment"
        android:label="@string/menu_camera_control"
        tools:layout="@layout/fragment_camera_control" />

    <fragment
        android:id="@+id/navigation_video_stream"
        android:name="com.ptzcontroller.ui.stream.VideoStreamFragment"
        android:label="@string/menu_video_stream"
        tools:layout="@layout/fragment_video_stream" />

    <fragment
        android:id="@+id/navigation_connection"
        android:name="com.ptzcontroller.ui.connection.ConnectionFragment"
        android:label="@string/menu_connection"
        tools:layout="@layout/fragment_connection" />

    <fragment
        android:id="@+id/navigation_settings"
        android:name="com.ptzcontroller.ui.settings.SettingsFragment"
        android:label="@string/menu_settings"
        tools:layout="@layout/fragment_settings" />
</navigation>