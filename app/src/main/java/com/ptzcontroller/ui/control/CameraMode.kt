package com.ptzcontroller.ui.control

/**
 * Enum representing camera modes (RGB or IR/thermal)
 */
enum class CameraMode {
    RGB,    // Regular RGB daylight camera
    IR      // Infrared/thermal camera
}